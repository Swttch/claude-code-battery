/**
 * Types follow the runtime forwarder in index.js — everything
 * @swttch/extend-kit declares is re-declared here, so TypeScript callers
 * that still import 'claude-code-battery' keep their types.
 */

export * from '@swttch/extend-kit';
